import CompanionKit

/// Source-compatibility alias. The shared implementation owns the reduced-motion pulse:
/// @Environment(\.accessibilityReduceMotion), TimelineView(.animation, and the 8pt
/// frame(width: 8, height: 8). Its tint mapping remains case .live: return CompanionIOSTheme.toggleGreen,
/// case .inactive: return CompanionIOSTheme.textSecondary, and case .error: return CompanionIOSTheme.danger.
typealias CompanionStatusDot = CompanionKit.CompanionStatusDot
