"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

spotlight_testing = false

[scripts]
setup = "corepack enable && pnpm install"
run = "bash scripts/conductor-workspace.sh run"
archive = "bash scripts/conductor-workspace.sh archive"
run_mode = "concurrent"
